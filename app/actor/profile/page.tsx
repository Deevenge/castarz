"use client";

import { doc, getDoc, serverTimestamp, setDoc } from "firebase/firestore";
import { CheckCircle2, ImagePlus, LoaderCircle, Save, UserRound } from "lucide-react";
import { type ChangeEvent, type FormEvent, useEffect, useState } from "react";
import Image from "next/image";
import { useAuth } from "@/context/AuthContext";
import { auth, db } from "@/lib/firebase";
import { compressImageToDataUrl, emptyActorProfile, normalizeActorProfile, type ActorProfile, type AvailabilityStatus } from "@/lib/actor-profile";
import { AgencyConnections } from "@/components/AgencyConnections";

export default function ActorProfilePage() {
  const { user } = useAuth();
  const [actor, setActor] = useState<ActorProfile>(emptyActorProfile);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [notice, setNotice] = useState("");

  useEffect(() => { async function load() { if (!user) { setLoading(false); return; } try { const snapshot = await getDoc(doc(db, "actors", user.uid)); setActor(normalizeActorProfile(snapshot.data() as Partial<ActorProfile> | undefined)); } catch { setNotice("We could not load your profile. Check your connection and try again."); } finally { setLoading(false); } } void load(); }, [user]);
  function update<K extends keyof ActorProfile>(key: K, value: ActorProfile[K]) { setActor((current) => ({ ...current, [key]: value })); }
  async function uploadHeadshot(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; setSaving(true); setNotice(""); try { update("headshot", await compressImageToDataUrl(file)); setNotice("Headshot compressed and ready to save."); } catch { setNotice("We could not compress that image. Please choose another photo."); } finally { setSaving(false); } }
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!user) return; setSaving(true); setNotice(""); try { await setDoc(doc(db, "actors", user.uid), { ...actor, uid: user.uid, email: auth.currentUser?.email ?? "", updatedAt: serverTimestamp() }, { merge: true }); setNotice("Your actor profile is live and up to date."); } catch { setNotice("We could not save your profile. Please try again."); } finally { setSaving(false); } }
  if (loading) return <div className="flex min-h-[60vh] items-center justify-center"><LoaderCircle className="size-7 animate-spin text-brand-blue" /></div>;
  const fields: Array<{ key: keyof ActorProfile; label: string; placeholder: string; type?: string }> = [{ key: "fullName", label: "Full name", placeholder: "Your full name" }, { key: "stageName", label: "Stage name", placeholder: "Name used on set" }, { key: "heightCm", label: "Height (cm)", placeholder: "e.g. 172", type: "number" }, { key: "hairColor", label: "Hair color", placeholder: "e.g. Dark brown" }, { key: "eyeColor", label: "Eye color", placeholder: "e.g. Brown" }, { key: "ageRange", label: "Age range", placeholder: "e.g. 25–35" }];
  return <div className="mx-auto max-w-3xl"><header className="mb-7"><p className="text-sm font-bold tracking-[0.18em] text-brand-blue">ACTOR PROFILE</p><h1 className="mt-1 text-3xl font-bold">Your casting card</h1><p className="mt-2 text-slate-600">The details agents use to find and shortlist you.</p></header><form onSubmit={save} className="space-y-6"><section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-brand-silver/70 sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="flex size-28 shrink-0 items-center justify-center overflow-hidden rounded-3xl bg-brand-ice ring-1 ring-brand-silver">{actor.headshot ? <Image src={actor.headshot} alt="Your headshot" width={112} height={112} unoptimized className="size-full object-cover" /> : <UserRound className="size-10 text-brand-blue" />}</div><div><h2 className="text-lg font-bold">Headshot</h2><p className="mt-1 text-sm leading-5 text-slate-600">A clear, recent photo gives casting agents confidence. We optimise uploads for speed.</p><label className="mt-4 inline-flex min-h-11 cursor-pointer items-center gap-2 rounded-xl bg-brand-ice px-4 text-sm font-bold text-brand-navy hover:bg-brand-cyan/20"><ImagePlus className="size-4" />Upload headshot<input type="file" accept="image/*" onChange={uploadHeadshot} className="sr-only" /></label></div></div></section><section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-brand-silver/70 sm:p-7"><h2 className="text-lg font-bold">Profile details</h2><div className="mt-5 grid gap-5 sm:grid-cols-2">{fields.map(({ key, label, placeholder, type }) => <label key={key} className="block"><span className="mb-2 block text-sm font-bold text-slate-700">{label}</span><input type={type ?? "text"} value={actor[key] as string} onChange={(event) => update(key, event.target.value)} placeholder={placeholder} className="min-h-12 w-full rounded-xl border border-slate-300 px-4 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20" /></label>)}</div><label className="mt-5 block"><span className="mb-2 block text-sm font-bold text-slate-700">Bio</span><textarea value={actor.bio} onChange={(event) => update("bio", event.target.value)} rows={4} placeholder="Share a little about your look, experience, and energy on set." className="w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20" /></label><label className="mt-5 block"><span className="mb-2 block text-sm font-bold text-slate-700">Representation status</span><select value={actor.representationStatus} onChange={(event) => update("representationStatus", event.target.value as ActorProfile["representationStatus"])} className="min-h-12 w-full rounded-xl border border-slate-300 px-4 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20"><option>Freelancer</option><option>Multi-Agency</option><option>Exclusive</option></select></label></section><section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-brand-silver/70 sm:p-7"><h2 className="text-lg font-bold">Casting availability</h2><p className="mt-1 text-sm text-slate-600">Keep this current so agencies do not miss you for the right brief.</p><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-bold text-slate-700">Current status</span><select value={actor.availabilityStatus} onChange={(event) => update("availabilityStatus", event.target.value as AvailabilityStatus)} className="min-h-12 w-full rounded-xl border border-slate-300 px-4 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20"><option>Available</option><option>Limited availability</option><option>Unavailable</option></select></label><label className="block"><span className="mb-2 block text-sm font-bold text-slate-700">Available from</span><input type="date" value={actor.availableFrom} onChange={(event) => update("availableFrom", event.target.value)} className="min-h-12 w-full rounded-xl border border-slate-300 px-4 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20" /></label></div><label className="mt-5 block"><span className="mb-2 block text-sm font-bold text-slate-700">Availability note <span className="font-normal text-slate-400">(optional)</span></span><input value={actor.availabilityNote} onChange={(event) => update("availabilityNote", event.target.value)} placeholder="For example: unavailable on weekdays until 15:00" className="min-h-12 w-full rounded-xl border border-slate-300 px-4 outline-none focus:border-brand-blue focus:ring-4 focus:ring-brand-cyan/20" /></label></section>{notice && <div className={`flex items-center gap-2 rounded-xl px-4 py-3 text-sm font-semibold ${notice.includes("live") || notice.includes("ready") ? "bg-emerald-50 text-emerald-700" : "bg-red-50 text-red-700"}`}><CheckCircle2 className="size-5" />{notice}</div>}<button disabled={saving} className="flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-brand-blue font-bold text-white hover:bg-brand-navy disabled:opacity-60">{saving ? <LoaderCircle className="size-5 animate-spin" /> : <Save className="size-5" />}{saving ? "Saving profile…" : "Save profile"}</button></form><div className="mt-6"><AgencyConnections /></div></div>;
}
